class TrainingCard {
  String expandedValue;
  String headerValue;
  bool isExpanded;

  TrainingCard(
      {required this.expandedValue,
      required this.headerValue,
      this.isExpanded = false});
}
